var nomePessoa = "Guilherme";

const emailPessoa = "gui@gmail.com";

let telefonePessoa = "11966638879"


// As variáveis no javascript, possuem escopos, cada escopo possui seu metodo de utilização;
// Esses escopos possuem nomes e suas caracteristicas, escopo global, função e bloco.

// Escopo Global, seria o escopo onde a variával pode ser utilizada em qualquer momento ou parte do código.

// Escopo de Função, seria a variavel que pode ser usada somente dentro de uma função.

// Escopo de Bloco, pode ser usada somente dentro de chaves, if, else, laços de repetição.

{
    console.log(nomePessoa);
    console.log(emailPessoa);
    console.log(telefonePessoa);
}